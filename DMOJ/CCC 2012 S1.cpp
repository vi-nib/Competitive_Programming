#include <bits/stdc++.h>
using namespace std;

int main() {

    ios::sync_with_stdio(0);
    cin.tie(0);

    int j;
    cin >> j;

    int calculation = (j - 3) * (j - 2) * (j - 1) / 6;

    cout<<calculation;

    return 0;

}